# Modal API URL — set after: python -m modal deploy modal/app.py
MODAL_API_URL=

# Hugging Face token (local model download only; Modal uses dashboard secret)
# HF_TOKEN=

# Optional overrides for local GPU eval (scripts/run_zero_shot_eval.sh)
# MODEL_PATH=models/LocateAnything-3B
# GENERATION_MODE=hybrid
# GPUS=1
